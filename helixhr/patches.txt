[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations

[post_model_sync]
# P5-U1: replaces the HR Request category Select options with records named
# exactly as the stored legacy values, so Link validation does not orphan rows.
helixhr.patches.v1_0.seed_request_categories
# P5-U2: Role fixtures sync before post-model patches, so the seeded IT / Asset
# route can now point to the portal-only IT Team role without rewriting history.
helixhr.patches.v1_0.route_it_asset_requests
# Patches added in this section will be executed after doctypes are migrated
# P2-U1 step 4: lists the legacy docstatus 0 / status Approved rows the
# pre-P2-U1 approval path left behind. Reports only -- it never submits.
helixhr.patches.v1_0.report_unsubmitted_approved_leave
# P2-U1: replaces the three Custom DocPerm fixtures. Snapshots this site's own
# standard DocPerm rows into Custom DocPerm, then applies only this app's
# permission deltas -- shipping them as fixtures stripped every role the
# fixture filter did not name.
helixhr.patches.v1_0.apply_permission_deltas
# P3-KTD13: same module, new deltas (Employee Checkin, Attendance Request).
# Frappe skips a patch whose exact line is already in the Patch Log, so a
# migrated site needs a fresh line to re-run it; the module is idempotent.
helixhr.patches.v1_0.apply_permission_deltas #2026-09-09 P3-KTD13 Employee Checkin and Attendance Request
# P4-KTD1: the two workflows' "Rejected" state now means a final no, and the
# recoverable meaning moves to a new "Sent Back". Renames existing docstatus-0
# rows before `sync_fixtures` installs the new transitions.
helixhr.patches.v1_0.rename_sent_back_state
# P4-KTD7a: same module, new deltas (HR Manager at permlevel 1 on Timesheet and
# Attendance Request, for `helixhr_decision_reason`).
helixhr.patches.v1_0.apply_permission_deltas #2026-09-10 P4-KTD7a decision reason permlevel
# P4-KTD4: same module, new deltas (HR Manager at permlevel 1 on Leave
# Application, for `helixhr_stage`).
helixhr.patches.v1_0.apply_permission_deltas #2026-09-10 P4-KTD4 leave stage permlevel
# P5-U2: IT Team access to routed HR Requests and category records.
helixhr.patches.v1_0.apply_permission_deltas #2026-09-15 P5-U2 IT Team permissions
# P5-U4: fixture removal does not delete existing Notification records.
helixhr.patches.v1_0.retire_request_notifications
# P4-KTD11: the two default Email Templates the celebration reminders send
# from, inserted only if absent and never overwritten -- fixtures re-import
# on every migrate and would replace HR's edited copy. Also called from
# `install.py`, because `--install-app` marks patches complete without
# running them.
helixhr.patches.v1_0.seed_celebration_templates
# P5-U13: the default request-notification wording, as editable
# HelixHR Message Template rows -- inserted only if absent, never
# overwriting an HR edit, same shape as seed_celebration_templates above.
helixhr.patches.v1_0.seed_message_templates
